server:
  port: 8080

spring:
  application:
    name: blogapp

  #  db configurations
  datasource:
    url: jdbc:mysql://localhost:3306/blog_app
    username: root
    password: 123*Priyanka
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQLDialect


#Dialect
#driver classname

